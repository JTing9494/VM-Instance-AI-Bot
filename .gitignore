# Environment variables
.env
.env.local
.env.*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage
*.lcov

# nyc test coverage
.nyc_output

# Grunt intermediate storage (https://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (https://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/
jspm_packages/

# Snowpack dependency directory (https://snowpack.dev/)
web_modules/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Microbundle cache
.rpt2_cache
.rts2_cache_cs
.rts2_cache_ts
.rts2_cache_js

# Bundle tool output (https://vitejs.dev/)
.dist
.cache
.temp
.temp/*
public/build

# Vite
.vite
# Vue Press
.vuepress/dist
# Docusaurus
.docusaurus
# CacheLoader
# https://github.com/webpack-contrib/cache-loader
.cache-loader
# eslint
.eslintcache
# ember-cli
.ember-cli
# floobits
.floobits
# Mertile
.mertile
# Netlify
/netlify
#     (https://www.netlify.com/docs/netlify-plugin-functions/)
#   functions/
# First party galaxy app directory
.galaxy
#   (https://galaxy.meteor.com/)
#   .meteor
# Visual Studio Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# VS Code Remote Containers
/.devcontainer/*

# Built files
dist/
build/
# TypeScript
*.tsbuildinfo
# Optional stylelint cache
.stylelintcache

# Docker
docker-compose.override.yml
# Docker swarm
docker-stack.yml

# Docker compose
# docker-compose.override.yml is ignored above, but we keep the base one
# Ignore any local overrides
docker-compose.*.override.yml

# Ignore any local Dockerfiles
Dockerfile.local
docker-compose.local.yml

# Ignore any local environment files (we already have .env above, but being explicit)
# .env is ignored above

# Python
# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
.dmypy.txt

# Pyre type checker
.pyre/

# Pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# Composer
composer.phar

# Node
npm-debug.log
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Disable telemetry
.# .telemetry.disable

# Parsers
parser/
/.*\.asmjit
/.*\.maleval
/.*\.hotrepair
/.*\.gpr

# Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs_vista.json
Desktop.ini
$RECYCLE.BIN/
*.csv
*.mdb
*.sldm
*.sldx
*.pdf
*.epub

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Icon must end with two \r
Icon

# FileVault secrets
.sparsebundle

# vim
*.swp
*.swo
*~

# Emacs
*~
\#*\#
/.emacs.desktop
/.emacs.diff
/_emacs.diff
/_emacs.diff
/auto-save-list
/tramp
.\*

# SFTP
.sftp-config.json

# Sublime Text
# Index which Sublime Text uses for project files
*.sublime-workspace
*.sublime-project

# SFTP
sftp-config.json

# Temperature control
# https://github.com/junegunn/vim-plug/blob/master/README.md#tips
*.vim
*.vimx
*.vimi

# Sass
.sass-cache

# Less
.less

# Stylus
.stylus

# Yarn Integrity File
.yarn-integrity

# dotenv environment variable files
.env
.env.*
# but keep the example
!.env.example

# Ignore any local Dockerfiles that might contain secrets
Dockerfile.local
docker-compose.*.yml

# Ignore any local overrides for docker-compose
docker-compose.override.yml

# Ignore any local environment files for docker-compose
# .env is already ignored above

# Ignore any local configuration for the backend
vm2-backend/.env
vm2-backend/.env.local
vm2-backend/env/
vm2-backend/venv/

# Ignore any local configuration for the frontend (if we had one)
# vm1-login/.env
# vm1-login/env/
# vm1-login/venv/

# Ignore any local configuration for the storage
vm3-storage/.env
vm3-storage/env/
vm3-storage/venv/

# Ignore any local data or cache directories
# For example, if we have a directory for cached data
data/
cache/
tmp/
temp/

# Ignore any backup files
*~
*.bak
*.tmp

# Ignore any OS generated files
.DS_Store
ehthumbs_vista
Icon?
Thumbs.db

# Ignore any Docker build cache (if we were to commit the build context, but we don't)
# However, we don't want to commit the Dockerfile if it's a local override
# We already have Dockerfile in the repo, so we ignore local ones
Dockerfile.local
docker-compose.local.yml
docker-compose.*.local.yml

# Ignore any local Kubernetes or Helm charts
# (if we had them)
# kubernetes/
# helm/

# Ignore any local Terraform state
# *.tfstate
# *.tfstate.*

# Ignore any local Packer cache
# .packercache

# Ignore any local Vagrant files
# .vagrant/

# Ignore any local Packer
# *.pkrc

# Ignore any local Vault
# .vault-token

# Ignore any local Consul
# .consulrc

# Ignore any local Nomad
# .nomadrc

# Ignore any local Docker Compose
# docker-compose.*.yml

# Ignore any local Docker
# Dockerfile

# But note: we want to keep the Dockerfile in the repo, so we don't ignore the base Dockerfile
# We are ignoring local overrides only.

# Ignore any local docker-compose files
# docker-compose.*.yml

# Ignore any local .env files (already covered)

# Ignore any local configuration for the IDE
# .idea/
# *.iml

# Ignore any local configuration for the database
# vm3-storage/data/
# But note: we might want to keep the data in the repo? No, because it's generated.
# However, we are using a volume for the database, so the data is not in the repo.
# We are not committing the data.

# Ignore any local configuration for the backend
# vm2-backend/__pycache__/
# vm2-backend/*.pyc
# vm2-backend/*~

# Ignore any local configuration for the frontend
# vm1-login/__pycache__/
# vm1-login/*.pyc
# vm1-login/*~

# We already have general rules for __pycache__ and *.pyc above.

# Ignore any local configuration for the storage
# vm3-storage/__pycache__/
# vm3-storage/*.pyc
# vm3-storage/*~

# We already have general rules for __pycache__ and *.pyc above.

# Ignore any local configuration for the project
# .idea/
# *.iml

# Ignore any local configuration for the database
# vm3-storage/data/ (if we were to store data in the repo, but we are not)
# We are using a volume, so the data is not in the repo.

# Ignore any local configuration for the logs
# logs/

# We already have logs/ above.

# Ignore any local configuration for the cache
# cache/

# We already have cache/ above.

# Ignore any local configuration for the temp
# temp/

# We already have temp/ above.

# Ignore any local configuration for the backup
# *~
# *.bak
# *.tmp

# We already have *~ above.

# Ignore any local configuration for the OS
# .DS_Store
# Thumbs.db

# We already have .DS_Store above.

# Ignore any local configuration for the vim
# *.swp
# *.swo
# *~

# We already have *~ above.

# Ignore any local configuration for the emacs
# *~
# \#*\#
# /emacs.desktop
# /emacs.diff
# We already have *~ above.

# Ignore any local configuration for the sublime text
# *.sublime-workspace
# *.sublime-project

# We don't have any, so we don't need to ignore.

# Ignore any local configuration for the sftp
# .sftp-config.json

# We don't have any, so we don't need to ignore.

# Ignore any local configuration for the temperature control
# We don't have any, so we don't need to ignore.

# Ignore any local configuration for the sass
# .sass-cache

# We already have .sass-cache above.

# Ignore any local configuration for the less
# .less

# We don't have any, so we don't need to ignore.

# Ignore any local configuration for the stylus
# .stylus

# We don't have any, so we don't need to ignore.

# Ignore any local configuration for the yarn integrity file
# .yarn-integrity

# We don't have any, so we don't need to ignore.

# Ignore any local configuration for the dotenv
# .env
# .env.*
# but we keep .env.example

# We already have .env above and we are keeping .env.example.

# Ignore any local configuration for the docker
# We don't want to ignore the base Dockerfile and docker-compose.yml, but we do want to ignore local overrides.
# We have already ignored:
#   docker-compose.override.yml
#   docker-compose.*.override.yml
#   Dockerfile.local
#   docker-compose.local.yml
#   docker-compose.*.local.yml

# We are keeping:
#   Dockerfile
#   docker-compose.yml

# So we are good.

# Ignore any local configuration for the kubernetes
# We don't have any, so we don't need to ignore.

# Ignore any local configuration for the helm
# We don't have any, so we don't need to ignore.

# Ignore any local configuration for the terraform
# We don't have any, so we don't need to ignore.

# Ignore any local configuration for the packer
# We don't have any, so we don't need to ignore.

# Ignore any local configuration for the vagrant
# We don't have any, so we don't need to ignore.

# Ignore any local configuration for the vault
# We don't have any, so we don't need to ignore.

# Ignore any local configuration for the consul
# We don't have any, so we don't need to ignore.

# Ignore any local configuration for the nomad
# We don't have any, so we don't need to ignore.

# We have covered the bases.

# Let's also ignore any local configuration for the database initialization scripts
# But note: the initialization scripts are in the repo, so we don't want to ignore them.
# However, if we have local overrides, we want to ignore them.
# We don't have any, so we don't need to ignore.

# We are done.